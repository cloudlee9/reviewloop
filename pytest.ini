[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: 会碰真实依赖（真子进程、真 claude/codex），默认不跑；用 -m integration 显式授权
    slow: 要等真实时钟（跟进度流、空闲超时），默认跑；嫌慢用 -m "not slow" 摘掉
addopts = -m "not integration"
